import 'package:flutter/material.dart';
import 'package:flutter/animation.dart';

class TempAnimatedWidget extends StatefulWidget {
  const TempAnimatedWidget({Key? key}) : super(key: key);

  @override
  _TempAnimatedWidgetState createState() => _TempAnimatedWidgetState();
}

class _TempAnimatedWidgetState extends State<TempAnimatedWidget> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Listner Demo'),
      ),
      body: Container(
        child: Center(
          child: ,
        ),
      ),
    );
  }
}
